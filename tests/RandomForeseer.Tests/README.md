# RandomForeseer.Tests

Maintained headless regression tests. Run `dotnet test` from the repository root.
Setup, commands and shared fixture rules:
[docs/testing.md](../../docs/testing.md).

Coverage and isolation boundaries are documented on each test class. Skipped tests document their specific limitations.
